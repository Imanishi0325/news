<div class="container">
    <h4>投稿一覧</h4>
    <% @articles.each do |article| %> 
        <div class="article">
            <p><%= article.text %></p>
        </div>
    <% end %>
    <%= paginate(@articles) %>
</div>