<p class="comment-detail">
    <strong><%= link_to comment.user.nickname, "/users/#{comment.user_id}" %>：</strong>
    <%= comment.text %>
      <% if comment.user_id == current_user.id %>
          <strong>
            <%= link_to '削除', "/top/#{@article.id}/comments/#{comment.id}", method: :delete, remote: true %>
          </strong>
      <% end %>
</p>